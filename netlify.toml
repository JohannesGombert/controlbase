[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"

# Future Netlify Functions folder, only needed for WHOOP OAuth/server-side sync.
[functions]
  directory = "netlify/functions"
